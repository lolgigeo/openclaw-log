---
title: OpenClaw文档库搭建全记录
date: 2026-02-06 01:02:00
categories:
  - OpenClaw
tags:
  - Hexo
  - 项目总结
  - 踩坑记录
  - GitHub
---

# OpenClaw文档库搭建全记录

> 项目日期：2026-02-06  
> 完成人：贾维斯（AI助手）  
> 用户：Vincent Zhang

---

## 📋 需求清单

### 1. ✅ Hexo博客框架迁移
- **需求**：从Docsify迁移到Hexo静态博客框架
- **原因**：需要更好的分类、标签、归档功能
- **完成时间**：2026-02-06 07:54

### 2. ✅ 主题切换为Prontera
- **需求**：使用https://github.com/AngryPowman/hexo-theme-prontera主题
- **特点**：简约、优雅、支持中文
- **完成时间**：2026-02-06 07:54

### 3. ✅ GitHub仓库创建与自动推送
- **仓库名称**：openclaw-log
- **仓库地址**：https://github.com/lolgigeo/openclaw-log
- **自动推送**：每10分钟检查变更并推送
- **完成时间**：2026-02-06 08:03

### 4. ✅ 分类和标签页面修复
- **问题**：prontera主题缺少page.jade、category.jade、tag.jade布局
- **解决方案**：手动创建布局模板并添加自定义CSS
- **完成时间**：2026-02-06 08:22

### 5. ✅ 文档迁移
迁移以下文档到新Hexo站点：
- 系统架构全景图.md
- 中证红利指数未来五年走势预测.md
- 市场指数追踪.md
- CalShift提交分析.md
- Hexo文档库使用指南.md

---

## 🚧 踩坑记录

### 坑1：Hexo项目位置混乱

**问题描述**：
- 初始在`~/docs-hexo/`创建了新Hexo项目
- 但域名`md.zeelool.asia`指向`/root/website/hexo/`
- 导致网站无法访问新主题

**解决方案**：
- 将prontera主题和文档迁移到`/root/website/hexo/`
- 保留现有SSL配置和Nginx设置
- 删除或保留`~/docs-hexo/`作为备份

**经验教训**：
在创建新项目前，先检查现有配置和部署位置，避免重复工作。

---

### 坑2：Prontera主题渲染失败

**问题描述**：
生成的HTML显示为Jade模板源代码，而非渲染后的页面。

**错误信息**：
```html
extends partial/layout

block container
    div#body-container
        div#post-list.left
            include mixins/post
            +posts()
```

**根本原因**：
- Prontera主题使用Jade模板引擎
- Hexo项目缺少`hexo-renderer-jade`渲染器

**解决方案**：
```bash
cd /root/website/hexo
npm install hexo-renderer-jade --save
npx hexo clean
npx hexo generate
```

**警告信息**（可忽略）：
```
npm warn deprecated hexo-renderer-jade@0.5.0: 
hexo-renderer-jade has been deprecated. 
Please install hexo-renderer-pug and rename all *.jade files to *.pug.
```

**经验教训**：
- Hexo主题需要对应的渲染器（EJS/Jade/Pug/Stylus等）
- 根据模板文件扩展名判断需要安装的渲染器
- 老主题可能使用已废弃的包，但仍可正常工作

---

### 坑3：Node-sass编译失败

**问题描述**：
执行`npm install`时，node-sass编译失败：

```
gyp ERR! configure error 
gyp ERR! stack Error: Can't find Python executable "python"
npm error Build failed with error code: 1
```

**根本原因**：
- Prontera主题依赖node-sass（已过时）
- Node-sass需要Python 2.7和C++编译环境
- Node.js 22不兼容老版本node-sass

**解决方案**：
**跳过主题依赖安装**，因为：
- 主题已有预编译的CSS文件（`source/css/prontera.css`）
- 不需要重新编译SCSS
- 只需确保Hexo能渲染Jade模板即可

**经验教训**：
- 老主题可能有编译问题，优先检查是否有预编译资源
- Node-sass已被Dart Sass取代，现代项目应避免使用
- 主题选择应考虑维护状态和技术栈兼容性

---

### 坑4：分类和标签页面无内容

**问题描述**：
访问`/categories/`和`/tags/`页面，只显示标题，没有分类/标签列表。

**根本原因**：
Prontera主题缺少以下布局模板：
- `page.jade` - 独立页面布局
- `category.jade` - 单个分类归档
- `tag.jade` - 单个标签归档

**解决方案**：

**1. 创建page.jade**（支持分类和标签列表）：
```jade
extends partial/layout

block container
    div#post-container
        div.post
            article.post-block
                h1.post-title= page.title
                if page.date
                    div.post-info= date(page.date, config.date_format)
                div.post-content!= page.content
                
                if page.type === 'categories'
                    div.categories-list
                        each category in site.categories.toArray()
                            div.category-item
                                a(href=url_for(category.path))
                                    h3= category.name
                                    span.count (#{category.posts.length})
                
                if page.type === 'tags'
                    div.tags-list
                        each tag in site.tags.toArray()
                            a.tag-item(href=url_for(tag.path))
                                span.tag-name= tag.name
                                span.count (#{tag.posts.length})
```

**2. 创建category.jade和tag.jade**（单个分类/标签页面）

**3. 添加自定义CSS样式**（`source/css/custom.css`）：
- 分类列表：卡片式设计，左侧蓝色边框
- 标签列表：标签云样式，蓝色圆角按钮
- 响应式设计，移动端优化

**经验教训**：
- 简约主题可能缺少部分功能模板
- 需要根据Hexo变量（site.categories/tags）自行实现
- 主题选择应检查是否支持所需功能

---

### 坑5：Git子模块警告

**问题描述**：
```
warning: adding embedded git repository: themes/prontera
hint: You've added another git repository inside your current repository.
```

**根本原因**：
- 从GitHub clone的主题包含`.git`目录
- Git将其识别为子模块（submodule）

**解决方案**：
```bash
rm -rf /root/website/hexo/themes/prontera/.git
git add -A
git commit -m "Fix: Remove git submodule"
```

**经验教训**：
- Clone第三方主题后应删除`.git`目录
- 或使用`git submodule`正确管理主题

---

### 坑6：Git用户信息未配置

**问题描述**：
```
fatal: unable to auto-detect email address
```

**解决方案**：
```bash
git config --global user.email "vincent.zhang@zeelool.com"
git config --global user.name "Vincent Zhang"
```

**经验教训**：
新环境需要先配置Git用户信息。

---

## 🎯 最终方案架构

### 目录结构
```
/root/website/hexo/
├── _config.yml              # 主配置（中文、时区、URL）
├── source/
│   ├── _posts/              # 文章目录
│   │   ├── 系统架构全景图.md
│   │   ├── 中证红利指数未来五年走势预测.md
│   │   ├── 市场指数追踪.md
│   │   ├── CalShift提交分析.md
│   │   └── Hexo文档库使用指南.md
│   ├── categories/index.md  # 分类页面
│   ├── tags/index.md        # 标签页面
│   └── css/custom.css       # 自定义样式
├── themes/
│   └── prontera/            # Prontera主题
│       ├── layout/
│       │   ├── page.jade    # 自建：独立页面布局
│       │   ├── category.jade # 自建：分类归档
│       │   └── tag.jade     # 自建：标签归档
│       └── _config.yml      # 主题配置（中文化）
├── public/                  # 生成的静态文件
├── auto-push.sh             # 自动推送脚本
└── package.json             # 依赖配置
```

### 自动化流程

**1. 内容更新流程**：
```
创建MD → 添加Front Matter → hexo generate → Nginx提供服务
```

**2. GitHub自动推送**：
```
Cron每10分钟 → 检测变更 → git commit → git push
```

**3. Nginx静态服务**：
```
请求 → Nginx → /root/website/hexo/public/ → 返回HTML
```

---

## 📊 技术栈总结

| 组件 | 技术选型 | 版本 |
|------|---------|------|
| 静态生成器 | Hexo | 8.1.1 |
| 主题 | Prontera | 1.0.0 |
| 模板引擎 | Jade (deprecated) | 1.11.0 |
| 渲染器 | hexo-renderer-jade | 0.5.0 |
| Web服务器 | Nginx | 1.24.0 |
| SSL证书 | Let's Encrypt | 自动续期 |
| 版本控制 | Git | - |
| 代码托管 | GitHub | lolgigeo/openclaw-log |
| 自动化 | Cron + Bash | - |

---

## 🔗 相关链接

- **在线访问**：https://md.zeelool.asia/
- **GitHub仓库**：https://github.com/lolgigeo/openclaw-log
- **Hexo官方文档**：https://hexo.io/zh-cn/docs/
- **Prontera主题**：https://github.com/AngryPowman/hexo-theme-prontera

---

## 📝 后续优化建议

### 短期优化
1. ✅ 添加Mermaid图表支持插件
2. ✅ 优化移动端响应式布局
3. ✅ 添加搜索功能（hexo-generator-search）
4. ✅ 配置sitemap（hexo-generator-sitemap）

### 长期优化
1. 考虑迁移到更现代的主题（如hexo-theme-next）
2. 升级模板引擎（Jade → Pug）
3. 添加评论系统（Disqus/Gitalk）
4. 集成CI/CD（GitHub Actions自动部署）
5. 添加访问统计（Google Analytics）

---

## 🎓 知识沉淀

### Hexo核心概念

**Front Matter（文章头部信息）**：
```yaml
---
title: 文章标题        # 必填
date: YYYY-MM-DD      # 必填
categories:           # 分类（支持多级）
  - 主分类
  - 子分类
tags:                 # 标签（支持多个）
  - 标签1
  - 标签2
---
```

**目录结构约定**：
- `source/_posts/` - 文章
- `source/<name>/index.md` - 独立页面（需设置`layout: page`）
- `themes/<name>/layout/` - 布局模板
- `public/` - 生成的静态文件（不提交Git）

**常用命令**：
```bash
hexo new "标题"        # 新建文章
hexo generate          # 生成静态文件（简写：hexo g）
hexo clean             # 清理缓存
hexo server            # 本地预览（端口4000）
hexo deploy            # 部署（需配置）
```

### Jade/Pug模板语法要点

**变量输出**：
```jade
h1= page.title              // 输出变量
div!= page.content          // 输出HTML（不转义）
```

**条件判断**：
```jade
if page.type === 'categories'
    div.categories-list
```

**循环**：
```jade
each category in site.categories.toArray()
    div.category-item
        h3= category.name
```

**模板继承**：
```jade
extends partial/layout      // 继承布局

block container             // 定义区块
    div 内容
```

### Hexo变量参考

- `site.categories` - 所有分类
- `site.tags` - 所有标签
- `site.posts` - 所有文章
- `page.title` - 当前页标题
- `page.type` - 页面类型
- `config.*` - 站点配置

---

## ⚠️ 注意事项

### 1. 文件编码
- 所有MD文件使用UTF-8编码
- 避免使用GBK等其他编码

### 2. Git忽略规则
`.gitignore`应包含：
```
node_modules/
public/
.deploy*/
db.json
*.log
.DS_Store
```

### 3. 安全考虑
- 不要提交包含敏感信息的配置
- SSH密钥仅用于Git推送，不要用于API认证
- 定期更新依赖包（`npm audit fix`）

### 4. 性能优化
- 静态资源CDN加速（可选）
- 图片压缩优化
- 开启Nginx GZIP压缩
- 静态资源缓存（已配置30天）

---

## 📞 问题反馈

如遇到问题，可通过以下方式反馈：
- GitHub Issues：https://github.com/lolgigeo/openclaw-log/issues
- 直接联系贾维斯（AI助手）

---

**文档维护者**：Vincent Zhang (贾维斯)  
**最后更新**：2026-02-06 01:02 UTC  
**文档版本**：v1.0
